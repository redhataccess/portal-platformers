{
  "name": "portal-platformers-game",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "build": "babel client --out-dir build --copy-files --presets=env --ignore=node_modules && cp -r node_modules build"
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "console-stamp": "^0.2.5",
    "express": "^4.15.3",
    "socket.io": "^2.0.3",
    "socket.io-client": "^2.0.3"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-env": "^1.1.8",
    "browser-sync": "^2.18.6"
  }
}
